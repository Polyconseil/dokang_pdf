language: python

python:
    - 2.7
    - 3.3
    - 3.4
    - 3.5

install:
    - python -c "import setuptools.version;print(setuptools.version.__version__)"
    - pip install -U pip setuptools
    - python -c "import setuptools.version;print(setuptools.version.__version__)"
    - pip install -r requirements_dev.txt
    - pip install --editable .
    - pip freeze

script:
    nosetests

notifications:
    email: true
